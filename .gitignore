.qodo

node_modules
dist
coverage

# Deploy artifact built by .github/workflows/deploy-appservice.yml
app.zip

.env
.env.local
.env.*.local
tokens.json

start.cmd

# Local capture output (may contain real account balances)
capture-*.txt

